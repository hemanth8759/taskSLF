{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "build": "cd taskSLF && ng build --prod",
    "installClient": "cd taskSLF && npm install",
    "heroku-postbuild": "npm run installClient && npm run build",
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "hemanth.k",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.0.1",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^5.12.3",
    "mongoose-unique-validator": "^2.0.3",
    "path": "^0.12.7"
  },
  "engines": {
    "node": "12.20.1",
    "npm": "7.7.4"
  }
}
